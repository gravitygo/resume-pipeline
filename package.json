{
  "name": "resume-pipeline",
  "version": "1.0.0",
  "description": "AI-powered resume generation system that transforms structured career data into polished, ATS-friendly resumes.",
  "license": "ISC",
  "author": "gravitygo",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "resume-pipeline": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node dist/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.5",
    "marked": "^12.0.0",
    "openai": "^4.28.0",
    "puppeteer": "^22.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
